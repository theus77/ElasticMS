{
   "acknowledged": {% if errors|length > 0 %}false{% else %}true{% endif %}
   {% if errors|length > 0 %}
		,"errors": [
	{% for error in errors %}
		{
			"propertyPath": {{ (form.vars.id~'_'~(error.cause.propertyPath|replace({'children[':'', ']':'', '.': '_'})))|json_encode|raw }}
		}
	{% endfor %}
	]   
	{% endif %}
}