{% extends 'base.html.twig' %}

{% block title %}List templates for {{ contentType.name }}{% endblock %}
{% block pagetitle %}List templates for content type {{ contentType.name }}{% endblock %} 


{% block body %}
<div class="row">
	<div class="col-md-12">
		<div class="box ">
			<div class="box-header with-border">
				<i class="fa fa-sitemap"></i>
				<h3 class="box-title">Templates for content type {{ contentType.name }}</h3>
				<div class="btn-group  pull-right">
					<a class="btn btn-xs btn-primary" href="{{ url('template.add', {'type':contentType.name }) }}"><i class="fa fa-plus"></i> Add template</a>
				</div>
			</div>
			<!-- /.box-header -->
			<div class="box-body">
				<table class="table table-condensed table-striped">
					<tbody>
						<tr>
							<th class="text-center" style="width: 10px">#</th>
							<th class="text-center" style="width: 20px">Icon</th>
							<th>Name</th>
							<th class="text-center" style="width: 30px">Link</th>
							<th>Action</th>
						</tr>
						{% for template in contentType.templates %}
							<tr>
								<td class="text-center">{{ loop.index }}.</td>
								<td class="text-center"><i class="{% if template.icon %}{{ template.icon }}{% else %}fa fa-file-text-o{% endif %}"></i></td>
								<td>{{ template.name|humanize }}</td>
								<td class="text-center">{% if template.externalLink %}<i class="fa fa-check"></i>{% endif %}</td>
								<td>
								
									<div class="btn-group">
{# 										{% include 'elements/get-button.html.twig' with {#}
{# 											'url':  path('contenttype.edit', {'id': contentType.id}),#}
{# 											'label': 'Edit',#}
{# 											'icon': 'pencil'#}
{# 										}%}#}
{# 										{% include 'elements/get-button.html.twig' with {#}
{# 											'url': path('template.index', {'type': contentType.name}),#}
{# 											'label': 'Templates',#}
{# 											'icon': 'file-text-o'#}
{# 										}%}#}
{# 										{% if contentType.environment.managed %}#}
{# 											{% include 'elements/post-button.html.twig' with {#}
{# 												'url':  path('contenttype.refreshmapping', {'id': contentType.id}),#}
{# 												'message': 'Try to update the ' ~ contentType.name|humanize ~ ' mapping ?',#}
{# 												'label': 'Update mapping',#}
{# 												'icon': 'refresh'#}
{# 											}%}#}
{# 										{% endif %}#}
{# 										{% if not contentType.active %}#}
{# 											{% include 'elements/post-button.html.twig' with {#}
{# 												'url':  path('contenttype.activate', {'id': contentType.id}),#}
{# 												'message': 'Are you ready to activate ' ~ contentType.name|humanize ~ ' ?',#}
{# 												'label': 'Activate',#}
{# 												'icon': 'warning'#}
{# 											}%}#}
{# 										{% endif %}#}
{# 										{% include 'elements/post-button.html.twig' with {#}
{# 											'url': path('contenttype.remove', {'id': contentType.id}),#}
{# 											'message': 'Delete the content type ' ~ contentType.name|humanize ~ ' ?',#}
{# 											'label': 'Delete',#}
{# 											'icon': 'trash'#}
{# 										}%}#}
				                    </div>
								
              
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>

			</div>
			<!-- /.box-body -->
		</div>
	</div>
</div>
{% endblock %} 


{% block javascripts %}
	{% include 'app/menu.html.twig' with {
		'item':  'contenttype-list'
	}%}
{% endblock %}	
